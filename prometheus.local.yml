global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "xc_api"
    dns_sd_configs:
      - names:
          - "host.docker.internal"
        type: "A"
        port: 8888